spring:
  profiles:
    active:
      - dev
---
spring:
  profiles: dev
  # application:
  #   name: springcloud-study-dev
---
spring:
  profiles: test
  # application:
  #   name: springcloud-study-test

# 必须保存为UTF-8编码
